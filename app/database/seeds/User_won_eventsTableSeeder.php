<?php

class User_won_eventsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('user_won_events')->truncate();

		$user_won_events = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('user_won_events')->insert($user_won_events);
	}

}
