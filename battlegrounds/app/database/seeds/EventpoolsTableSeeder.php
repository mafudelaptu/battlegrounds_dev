<?php

class EventpoolsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('eventpools')->truncate();

		$eventpools = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('eventpools')->insert($eventpools);
	}

}
