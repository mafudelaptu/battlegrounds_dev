<?php

class Matchreplay_chatsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('matchreplay_chats')->truncate();

		$matchreplay_chats = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('matchreplay_chats')->insert($matchreplay_chats);
	}

}
